{
  "name": "hillclimb-monorepo",
  "private": true,
  "description": "CI for agent prompt changes",
  "scripts": {
    "test": "cd tests && python -m pytest -v",
    "mock": "python mock-services/app.py",
    "cli:build": "cd packages/cli && npm run build",
    "cli:dev": "cd packages/cli && npm run dev"
  }
}
